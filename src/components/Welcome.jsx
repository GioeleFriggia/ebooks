import React from "react";
import { Alert } from "react-bootstrap";

const Welcome = () => (
  <Alert className="text-center bg-light border border-danger">
    <h1>Benvenuti in EpiBooks!</h1>
  </Alert>
);

export default Welcome;
